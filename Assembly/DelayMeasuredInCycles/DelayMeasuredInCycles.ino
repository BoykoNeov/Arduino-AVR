extern "C" 
{
    // Takes as input (uint16) x cycles and returns after (x times 4 cycles) + around 9 , 0 is MaxValue;
  void DelayCyclesX6(uint32_t cyclesX6);
  
    // Takes as input (uint32) x cycles and returns after (x times 6 cycles) + around 11;
  void DelayCyclesX4(uint16_t cyclesX4);
}

void setup() {
}

void loop() 
{
}
